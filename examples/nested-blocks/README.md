# Nested blocks (Svgbob) examples

Source files consumed by **`backends/blocks/`** and the Studio web UI nested-blocks tab.

- **`nested.txt`** — multi-level ASCII block diagram sample.
- **`tables.md`** — Markdown tables rendered as multiple SVG sheets.
