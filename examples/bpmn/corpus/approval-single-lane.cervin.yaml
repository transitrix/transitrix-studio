# CORPUS CELL: M-Lo-A-Lo-1
# Element count: 11 (8 tasks, 2 events, 1 XOR gateway)
# Cross-lane flows: 0/10 (0%)
# Cycles: none (acyclic)
# Gateway density: 9% (1/11 elements)
# Lanes: 1 (single lane; same-lane only)
# Purpose: Medium same-lane diagram; tests gateway port distribution in single lane
# Source: synthetic example with conditional branching

process:
  id: process-approval-single-lane
  name: Approval Single Lane Process
  pools:
    - id: pool-approval-single-lane
      name: ApprovalSingleLane
      lanes:
    - id: lane-all
      name: Main Process
      elements:
        - id: start
          type: startEvent
          name: Request Received
        - id: validate
          type: task
          name: Validate Request
        - id: gw-valid
          type: exclusiveGateway
          name: Valid?
        - id: process-ok
          type: task
          name: Process
        - id: notify-approve
          type: task
          name: Send Approval
        - id: reject
          type: task
          name: Reject Request
        - id: notify-reject
          type: task
          name: Send Rejection
        - id: log-approved
          type: task
          name: Log Approval
        - id: log-rejected
          type: task
          name: Log Rejection
        - id: archive
          type: task
          name: Archive
        - id: end
          type: endEvent
          name: Done

