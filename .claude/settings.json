{
  "permissions": {
    "allow": [
      "Edit(intellij/gradle/wrapper/gradle-wrapper.properties)",
      "Write(intellij/gradle/wrapper/gradle-wrapper.properties)",
      "Edit(intellij/gradle.properties)",
      "Write(intellij/gradle.properties)"
    ]
  },
  "autoMode": {
    "allow": [
      "$defaults",
      "Edit or Write to intellij/gradle/wrapper/gradle-wrapper.properties or intellij/gradle.properties in this repo when the change is a Gradle distributionUrl/gradleVersion version bump (not adding new download hosts or unrelated content) -- pre-approved per transitrix-hq#129: three independent unattended runs already diagnosed and tested this exact fix, and the harness's sensitive-file gate otherwise leaves no unattended path to land it."
    ]
  }
}
