# Location — worked examples

Business-layer locations (`notation: location`). Schema: methodology
`notations/elements/21-locations.md`.

Hand-authored — no `LOCATION` element exists yet in the acme-corp worked
example; consumed by `tests/validate-notation.test.ts`.

- `LOCATION-GE-1.yaml` — country, no `parent`.
- `LOCATION-TBILISI-1.yaml` — office, `parent: LOCATION-GE-1`.
