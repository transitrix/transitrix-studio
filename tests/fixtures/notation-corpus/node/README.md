# Node — worked examples

Technology-layer infrastructure substrate (`notation: node`). Schema:
`notations/elements/25-nodes.md` §2.

Hand-authored — no promoted `NODE` element exists yet in the acme-corp
worked example; consumed by `tests/validate-notation.test.ts`.

- `NODE-KAFKA-HOST-1.yaml` — `cloud_instance`, AWS-hosted.
- `NODE-DB-PRIMARY-1.yaml` — `database_server`, on-premises.
