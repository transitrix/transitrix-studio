# Business Service — worked examples

Business-layer services (`notation: business-service`). Schema: methodology
`notations/elements/25-business-services.md`.

Hand-authored — no `BUSINESS_SERVICE` element exists yet in the acme-corp
worked example; consumed by `tests/validate-notation.test.ts`.

- `BUSINESS_SERVICE-ONBOARDING-1.yaml` — `internal`, with `offering_unit` (ACTOR) and `capability`.
- `BUSINESS_SERVICE-SUPPORT-1.yaml` — `external`, `offering_unit` is a ROLE, no `capability`.
