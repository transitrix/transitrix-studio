# Technology service — worked examples

Technology-layer platform service exposed by a NODE (`notation:
technology-service`). Schema: `notations/elements/26-technology-services.md`
§2.

Hand-authored — no promoted `TECHNOLOGY_SERVICE` element exists yet in the
acme-corp worked example; consumed by `tests/validate-notation.test.ts`.

- `TECHNOLOGY_SERVICE-ORDER-API-GW-1.yaml` — `api_gateway`.
- `TECHNOLOGY_SERVICE-OMS-DATABASE-1.yaml` — `database`.
