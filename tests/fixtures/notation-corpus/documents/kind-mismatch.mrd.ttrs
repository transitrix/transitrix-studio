---
document: Software Requirements Specification
kind: srs
recipe_id: platform.srs
recipe_version: "1.0"
---

# Kind disagreement

The filename's kind segment says `mrd`; the header above says `srs`. Exactly one
of the two is wrong, and the validator says so rather than guessing which.
