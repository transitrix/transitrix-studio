# PlantUML fixture

`sample.puml` — a minimal sequence diagram, authored for transitrix-hq#143
(hold 6 verification) to exercise `extension/src/plantuml-preview.ts`. No
`.puml`/`.plantuml` fixture existed in the notation corpus before this; every
other directory here mirrors a fixture already used elsewhere in the repo,
this one does not.
