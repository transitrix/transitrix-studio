{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "extension-prep",
      "type": "shell",
      "command": "npm run extension:prep",
      "options": { "cwd": "${workspaceFolder}" },
      "group": "build",
      "presentation": {
        "reveal": "silent",
        "panel": "shared"
      }
    }
  ]
}
